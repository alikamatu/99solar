express = require('express');
const router = express.Router();
const uploadLots = require('../controllers/lotController');


router.post('/upload', uploadLots);
// router.get('/lots', lotsAll);
// router.get('/lots/:id', lotById);

module.exports = router;